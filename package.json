{
  "name": "webuildsg-assets",
  "version": "1.0.0",
  "description": "common logo, icons and css for webuild.sg related sites",
  "main": "index.html",
  "scripts": {
    "build": "stylus css/style.styl --compress",
    "clean": "rm css/style.css",
    "deploy": "git push origin gh-pages",
    "start": "parallelshell 'http-server -p 8888' 'npm run watch'",
    "test": "echo All good!",
    "watch": "npm run clean && stylus css/style.styl --compress --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/webuildsg/assets.git"
  },
  "keywords": [
    "assets",
    "css",
    "stylus",
    "logo",
    "favicon"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/webuildsg/assets/issues"
  },
  "homepage": "https://github.com/webuildsg/assets#readme",
  "devDependencies": {
    "csslint": "^1.0.4",
    "http-server": "^0.9.0",
    "parallelshell": "latest",
    "stylus": "^0.54.5"
  }
}
